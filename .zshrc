export PATH="/path/to/python3/bin:$PATH"
export PATH="$PATH:/usr/local/bin"
export PATH="/opt/homebrew/opt/python@3.12/bin:$PATH"
export PATH=$PATH:/Users/marcinosienienko/.nvm/versions/node/v22.5.1/bin
export PATH="$PATH":"$HOME/.pub-cache/bin"
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

## [Completion]
## Completion scripts setup. Remove the following line to uninstall
[[ -f /Users/marcinosienienko/.dart-cli-completion/zsh-config.zsh ]] && . /Users/marcinosienienko/.dart-cli-completion/zsh-config.zsh || true
## [/Completion]

